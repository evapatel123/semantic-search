# 🔎 Semantic Search
First, make a new copy of this notebook. Next, go to `Edit` and select `Clear all outputs`. This will make sure that when you run your code, the output is not already shown.

This notebooks shows how to complete semantic search by calculating cosine similarity with the help of PyTorch. These are the steps needed to complete semantic search:

1️⃣ Import the Sentence Transformers library.

2️⃣ Load the document.

3️⃣ Break up the text into chunks.

4️⃣ Convert those chunks into vectors.

5️⃣ Create a function that accepts a user's query, converts that querty into a vector, normalizes the vectors, and computes cosine similarity to return the relevant sentences.

6️⃣ Test the semantic search by calling the function.
